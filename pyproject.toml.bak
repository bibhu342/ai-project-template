### Put this into **pyproject.toml** (tooling: Black + Ruff)
```toml
[project]
name = "ai-project-template"
version = "0.1.0"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E","F","I","B"]